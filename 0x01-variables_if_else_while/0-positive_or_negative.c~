#include <stdio.h>
/**
 *main - Ths should output if the nomber is positive or negative
 *Return: this should return 0
int main(void)
{
#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	return (0);
}
}